import UnAuthorized from '@/components/ui/UnAuthorized';
import type { ReactNode } from 'react';

const UnauthorizedPage = (): ReactNode => {
  return <UnAuthorized className="" />;
};

export default UnauthorizedPage;
